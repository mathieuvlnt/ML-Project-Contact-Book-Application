# ML-Project-Contact-Book-Application
The project's goal is to create a Contact Book application that runs entirely in the terminal (command-line interface). Users should be able to add, search, update, delete, and view contact information. The app must store contacts in a file so data persists across runs. This simulates a basic database and CRUD (Create, Read, Update, Delete) system.
